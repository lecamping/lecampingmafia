{% if ('delete' in actions) and (not hide_delete) %}<form action="{{ "{{ path('"~ route_name_prefix ~"_delete', { 'id': entity.id }) }}" }}" method="post">{{ '{{ form_widget(delete_form) }}' }}<button type="submit">Delete</button></form>{% endif %}
<a href="{{ "{{ path('"~ route_name_prefix ~"') }}" }}">Back to the list</a>
{% if ('edit' in actions) and (not hide_edit) %}&nbsp;&nbsp;&nbsp;&nbsp;<a href="{{ "{{ path('"~ route_name_prefix ~"_edit', { 'id': entity.id }) }}" }}">Edit</a>{% endif %}

