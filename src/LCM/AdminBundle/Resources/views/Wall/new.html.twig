{% extends 'LCMAdminBundle::layout.html.twig' %}

{% set title = 'Wall' %}
{% set subtitle = 'creation' %}

{% block actions %}

<a href="{{ path('wall') }}">Back to the list</a>


{% endblock %}

{% block content %}


<form action="{{ path('wall_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>


{% endblock %}
